Blockly.JavaScript['move_block'] = function(block) {
  var number_second_to_move = block.getFieldValue('Second to Move');
  var checkbox_forwards_ = block.getFieldValue('Forwards?') === 'TRUE';
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['turn_block'] = function(block) {
  var angle_degrees = block.getFieldValue('Degrees');
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['starter'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};

Blockly.JavaScript['end'] = function(block) {
  // TODO: Assemble JavaScript into code variable.
  var code = '...;\n';
  return code;
};